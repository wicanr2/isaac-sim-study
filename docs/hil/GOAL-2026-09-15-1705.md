# GOAL 2:受控體換成 Isaac 6.0.1、Renode 三項修正送上游、上位接 ROS 2 Jazzy

產生時間:2026-09-15 17:05。接續 [GOAL 1](GOAL-2026-09-15-1538.md)(已完成、已 push);決定與結果回填在 [issue #1](https://github.com/wicanr2/isaac-sim-study/issues/1)。

## 你是誰、在哪
- repo:`isaac-sim-study`(公開)。先讀 issue #1 與 [38 篇](38-acceptance-and-failure-modes/README.md) §6–§7,不重問已定案的事。
- `git config user.email` 必須是 `wicanr2@gmail.com`。
- 起點:`examples/hil-stm32/run_loop.sh` 本機 ALL PASS。**先跑一次確認還是綠的。**

## 目標(一句話)
把 38 篇 §7 的分階段往下推:**受控體換成 Isaac 6.0.1 並實測**(拿掉「未驗證」),**Renode 三項缺口修掉送上游**,上位接 **ROS 2 Jazzy**。

## 三條線(A 主線;B、C 不擋 A)

### A. Isaac 6.0.1 受控體實測(場域 GPU 主機)
Renode + 橋接**留在本機 docker**,只有受控體上 GPU 主機(那台沒 docker、沒 root)。受控體協定加 TCP 傳輸,`tools/remote.sh tunnel` 一條 `ssh -L` 接上。
1. 橋接加 `Tcp` 受控體、`fake_plant.py` 加 `--tcp`;本機 fake 走 TCP 先綠
2. `isaac_plant.py` 上主機實跑,逐條回答 38 篇 §6 七件事,每條寫「量到什麼」。已知:`timeStepsPerSecond` 在 6.0 標 Deprecated([17 篇](../6.0.1/17-physics-parameter-tuning-6.0/README.md) §2)要讀回驗;後端已查證是 PhysX
3. C3 容差按滑移實測值重定,寫回判準與 38 篇 §1
4. 決定性:`enableGPUDynamics` 讀回;CSV 不同就量離散度定容差,不硬湊
5. 過的把 35/37/38 篇與腳本篇首的「未驗證」改成「實測於 …」附條件;沒過的留標記寫差在哪

### B. Renode 三項修正 + 上游 PR
對象 `renode-infrastructure` @ `add012af0…`(= 1.16.1)。

| 項 | 1.16.1 現況 | 改成 |
|---|---|---|
| `STM32_Timer` 週期 | = ARR(ARR=999 跑 10000 tick → CNT 10) | = ARR+1(RM0090) |
| `STM32_Timer` OC1PE/OC2PE | 未實作 | CCR 寫入下一個 update event 才生效 |
| `STM32_UART` TC | 清除後永不重設(既有內部專案實測) | 送完重設 1;TCIE 時拉中斷 |

順序:每項一支探針(原版紅)→ `i @file.cs` 執行期載入修正(秒級迴圈,不建 Renode)→ 修正版綠 → fork 改碼、補上游樣式的測試 → 自建 Renode 跑上游測試(**.NET 8 SDK、約 10 GB、20 分鐘以上、要網路,只做一次**)。**送 PR 前先問**;PR 英文,附量測與 RM0090 章節;36/37 篇引用連結標「已送上游 / 已合併」。

### C. 上位 ROS 2 Jazzy + realtime 模式
1. rclpy 節點:訂 `/cmd_vel` → 框包 → 橋接;odom → `/odom` + TF。跑 `ros:jazzy` docker(拉映像約 1 GB,先報)
2. 橋接加 `realtime`:Renode 自由跑,橋接以牆鐘 5 ms 取樣與注入;量三時鐘分歧,補進 35 篇 §5
3. `ros2 topic pub /cmd_vel` 讓假受控體走方形,`/odom` 閉環;與 lockstep 同腳本末端位姿差在容差內

## 工作順序
起點驗證 → A-1 → GPU 主機盤點 → rsync 腳本 + calib、單獨起 Isaac 看到 `listening` → A-2 逐條(每條一 commit)→ A-3/4/5 → B → C。每條線結束:`build_site.py` + `check_docs.py` 全綠、跨篇 §N 人工核、commit;push 前問。

## 硬規則
- GOAL 1 全部硬規則照舊。
- **[HARD] GPU 主機邊界**:只用 `tools/remote.sh`(主機、帳號從機密入口腳本推出);**主機名、IP、帳號、家目錄路徑不進 repo、log、對話、issue**,寫「場域 GPU 主機」;沒 docker/root,用既有 Isaac venv 與包裝腳本,不重裝;每次開跑前 `nvidia-smi`(含 vGPU 授權到期)、`uptime`、`ps -u`,**不拿上次快照當現況**;CPU 最多一半(4 核)、同時一個 Isaac、只寫 `~/hil-plant/`、rsync 不 clone;家目錄是 NFS,大檔同步一次做完。
- **[HARD] 缺口修在 Renode 本體,韌體照 RM0090 寫,不因模擬器改寫。**
- **[HARD] 每一條「未驗證 → 實測」都要附量測紀錄**(指令、環境、數字),沒紀錄不算升級。
- 送 PR、拉映像、在主機起 > 5 分鐘的長跑之前,先報數字再問。

## 完成的定義
- A:七件事每條有結論;標記依結果改寫;對 Isaac 至少一次 ALL PASS(容差重定後),負對照仍紅;決定性有結論
- B:三項各有「原版紅、修正綠」紀錄;fork 有測試;PR 已開(或使用者決定不送,理由寫進 37 篇 §4)
- C:方形閉環、三時鐘分歧數字、lockstep vs realtime 差在容差內
- `check_docs.py` 全綠;issue #1 回填;沒做到的寫明為什麼
