# GOAL 4:純軟體把差速底盤韌體補到真車的形狀——編碼器、加減速與馬達層、安全 I/O、Nav2

產生時間:2026-09-16 15:47。接續 [GOAL 3](GOAL-2026-09-16-1118.md)(四線完成、已 push;上游 PR #250、renode#1003/#1004 已送)。四條線各對應一個 issue:[#2 編碼器](https://github.com/wicanr2/isaac-sim-study/issues/2)、[#3 加減速與馬達層](https://github.com/wicanr2/isaac-sim-study/issues/3)、[#4 安全 I/O](https://github.com/wicanr2/isaac-sim-study/issues/4)、[#5 Nav2](https://github.com/wicanr2/isaac-sim-study/issues/5)。做法細節在 issue,這裡只放順序、規則與完成定義;結果回填到各 issue,總結回 [issue #1](https://github.com/wicanr2/isaac-sim-study/issues/1)。

## 你是誰、在哪
- repo:`isaac-sim-study`(公開)。先讀 issue #1 最後兩則與 #2–#5,不重問已定案的事。
- `git config user.email` 必須是 `wicanr2@gmail.com`(歷史已全部改寫,`git log --format=%ae | sort -u` 只能有這一個)。
- 起點:`examples/hil-stm32/run_loop.sh`、`FW=freertos`、`CAN=socketcan CANHUBFIX=1`、`UPPER=ros --seconds 40` 四個都 ALL PASS。**先跑一次。**
- 主機 14 核、常有別的 session(load 6–12);速度數字附 `uptime`;橋接守不住 5 ms 節拍是已知事實,不要再花時間證明。
- **沒有板子**:全部純軟體。場域 GPU 主機只在 B 線與 D 線的 Isaac 驗證用,規則同 GOAL 2(先盤點、CPU 一半、只寫 `~/hil-plant/`、主機資訊不進 repo)。

## 目標(一句話)
同一台車、同一套橋接與判準,把韌體從「教學級底盤控制器」推到「真車驅動板的形狀」,每一步都在純軟體裡驗到綠,並把路上踩到的 Renode 缺口修掉。

## 四條線(順序 A → B → C → D;A 是主線,後三條不擋 A 但等 A 的介面定案)

### A. 編碼器改走 TIM encoder mode(#2)
Renode 1.16.1 的 `STM32_Timer` 沒有 encoder mode,預期紅 → 修 Renode(NUnit + 探針原版紅/修正版綠)→ 橋接改驅動正交脈衝或寫 CNT(**三種注入法各量每步成本再選**)→ 兩版韌體讀 CNT → lockstep 全綠、負對照(兩路對調)紅、**realtime 同腳本對 lockstep 的差要能只用節拍統計解釋**。35 篇 §5.1 的開放項改寫成結論。

### B. 加減速限制、前饋、整定;馬達層加電流限制與摩擦(#3)
**先量步階響應再改**(假受控體 + Isaac 各一次,寫成基準表)。斜坡與前饋進 calib,兩版韌體同改;受控體三個實作(Rust Fake、Python、Isaac)同一份馬達公式。新增 C9(加速度上限)與負對照;Isaac 滑移與方形閉合誤差目標先量再定,不硬湊。

### C. 安全 I/O 五項(#4)
IWDG、驅動器故障腳、bumper、堵轉、心跳。每項:韌體 + 橋接/受控體那一側 + **一個 `--negative` 旗標**;`flags` 位元進 odom 與 CAN 狀態框。IWDG 先盤點 Renode 的看門狗模型逾時會不會 reset——不會就是缺口,修。新增 C10。

### D. Nav2 in the loop(#5)
受控體加假雷射(矩形房間 + 方塊,`SCAN` 一行),橋接只轉譯;driver 加 `/scan` 與靜態 TF;Nav2 最小 bringup。`NavigateToPose` 3 m 級到達 < 0.1 m、不撞方塊;負對照:`/scan` 全設最大距離 → 撞 → 紅。**拉 Nav2 映像(約 +2 GB)前報數字再問。**

## 工作順序
起點驗證 → A-盤點 → A-修 Renode → A-橋接注入法量測 → A-韌體 → A-驗收 → B-步階基準 → B → C(五項各一 commit)→ D。每條線結束:`build_site.py` + `check_docs.py` 全綠、對應 issue 回填、commit;**push 前問**。

## 硬規則
- GOAL 1–3 全部硬規則照舊(docker、匿名化、缺口修 Renode 不改韌體、每條升級附量測、長跑與拉映像前報數字、PR 英文且送前給看)。
- **[HARD] 韌體照 RM0090 寫。** encoder mode、IWDG 的暫存器序列以手冊為準;Renode 不支援就修 Renode,不在韌體加「模擬裡的替代路徑」。
- **[HARD] 每個新判準都要有讓它轉紅的負對照,先紅再綠。**
- **[HARD] 三個受控體實作同一份公式**(馬達層、假雷射):改一處要改三處,末端位姿要互相在容差內。
- **[HARD] 圖與正文同一份數字**;新的機制(encoder mode 計數規則、斜坡、安全狀態機、假雷射)各補一張 SVG。
- 場域 GPU 主機:每次開跑前 `nvidia-smi`、`uptime`、`ps`,不拿上次快照當現況。

## 完成的定義
- A:Renode encoder mode 修正(NUnit、探針)進 fork;兩版韌體讀 CNT;C1–C8 全綠、負對照紅;realtime 差有節拍統計解釋;35/36/37 篇改寫
- B:步階基準表;C9 與負對照;Isaac 滑移與方形閉合前後對照有數字
- C:五項各有正/負對照與 `io_check` 項;C10
- D:Nav2 到達與不撞的數字、負對照紅;realtime 節拍與 `[clocks]` 一起報
- 每個 issue 回填、issue #1 總結;`check_docs.py` 全綠;沒做到的寫明為什麼
