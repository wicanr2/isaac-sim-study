# <這批實驗的名稱> —— 事前登記

> **YYYY-MM-DD 寫,結果出來之前。**
> 觸發:<誰要求的、原話是什麼>
> 前置:<這批之前跑完的是哪一批>

## 1. 樣本數與它能問的問題

一輪約 <N> 小時。**這決定了能問什麼、不能問什麼。**

| 統計單位 | 一輪貢獻 | <N> 輪的樣本 | 全過時的 95% 信賴下界 |
|---|---|---|---|
| <細單位> | | | |
| <整輪> | | | |

單側二項下界:`0.05 ** (1/n)`。

🔴 結論:報告要分幾層寫、每層能宣稱什麼。

## 2. 組態(寫死,不得事後改)

```
<完整啟動指令與參數>
```

**與上一批的唯一差別是**:<...>

⚠ **不改變的觀測條件也要寫死**:<例如「逐幀拍攝維持開著」>
理由:<觀測手段會影響結果 / 影響可比較性>

## 3. 預期結果(互斥分支,寫在結果之前)

| 若觀察到 | 則 |
|---|---|
| | |
| | |
| | |

🔴 **判讀規則**:<例如「不要把 X 低於 Y 讀成退步,因為兩者測的不是同一件事」>

## 4. 停止規則

- <條件> → <動作>
- <條件> → <動作>

⚠ **停止規則要跟統計目的對齊。** 「快速失敗」與「累積樣本」是相反的需求。

## 5. 判讀方式

用 `<固定工具>`,**不手算**。接在 `<事件>` 上,判讀與進度同步發生。

## 6. 紀律

- 結果出來之前不改判準。
- 中途發現的新判準列為**系列結束後**候選,不套用到進行中的系列。
- **修壞掉的尺 ≠ 換一把尺。** 判斷方法:修完之後已跑的輪次要不要重判?
  要重判就是換尺,停下來重新登記。

---

## 結果(跑完之後填,上面不動)
