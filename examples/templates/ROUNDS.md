# 實驗輪次紀錄

> 一輪一列。**改動欄只寫「與上一輪的差異」**,不要重複全部設定。
> **結果欄寫可驗證的量測值,不寫感想。**
> 判定用固定工具(填入你的驗收腳本),不手算。

🔴 **這是編年流水帳,不是現況。**
每一列寫的是那一天當下的理解。**後面的輪次會推翻前面的結論,而且推翻得很頻繁。**
現況看〈一句話現狀〉,已被推翻的斷言看下面那張表。

## 一句話現狀

(每次結論改變時更新這一行。讀者只讀這一行也要拿到正確答案。)

## 已被推翻的斷言

| 原本以為 | 什麼推翻了它 | 日期 |
|---|---|---|
| | | |

## 輪次

| 輪 | 改動(與上一輪的差異) | 結果(可驗證的量測值) |
|---|---|---|
| X0 | 基線,`<參數> = <值>` | `<指標> = <值>`;`<指標> = <值>` |
| X1 | `<參數>` → `<新值>` | ✅ / 🔴 `<指標> = <值>` |

## 分期敘事

| # | 期間 | 主題 | 這一段最重要的一件事 |
|---|---|---|---|
| 1 | MM-DD ~ DD(連到 `rounds/01-....md`) | | |
